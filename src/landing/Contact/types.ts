export type ContactFormData = {
  name: string;
  phone: string;
  subject: string;
  message: string;
};

export type ContactInfoItem = {
  icon: React.ElementType;
  title: string;
  content: string;
};
